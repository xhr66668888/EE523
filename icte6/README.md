# Smart Tip Splitter

Kotlin Android implementation for ICTE6 Option 1.

The app accepts a bill amount, tax rate, tip percentage, and number of people. It validates the inputs and calculates:

- Tax amount
- Tip amount
- Final total
- Per-person split amount

Build from this folder with:

```bash
./gradlew test assembleDebug
```
